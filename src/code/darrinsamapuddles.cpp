#include <bits/stdc++.h>
using namespace std;

const int N = 1e5;
int n, q;
int x[N], A[N], G[N];

#define f(i,a,b) for (int i = a; i < b; i++)

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	cin>>n>>q;
	f(i,0,n) cin>>x[i]>>A[i]>>G[i];
	



	return 0;
}
