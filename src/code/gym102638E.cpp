#include <bits/stdc++.h>
using namespace std;

#define f(i,a,b) for (int i = a; i < b; i++)

const int N = 500000;

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int n; cin>>n;
	int a[n];
	f(i,0,n) a[i] = 0;

	f(i,0,n) {
		
	}

	return 0;
}
