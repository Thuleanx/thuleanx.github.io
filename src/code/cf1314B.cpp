#include <bits/stdc++.h>
using namespace std;

#define f(i,a,b) for (int i = a; i < b; i++)
#define mp make_pair

int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0);

	int n, k; 
	cin>>n>>k;

	vector<int> like;

	return 0;
}
